import styled from 'styled-components';

export const Link = styled.a`
    //компонента будет использоваться как другая компонента, поэтому пишем только то, что отличается
    color: #4983c3;
    font-size: 2rem;
    font-weight: bold;
    background-color: transparent;
    padding: 0;
`